{% extends 'base.html.twig' %}
{% block body %}
  <h1>{{ blog.title }}</h1>
  <p>{{ blog.description }}</p>
  <p>{{ blog.date|date("d-m-Y")  }}</p>

  {% if comments %}
    {% for comment in comments %}
    <div class="card">
      <div class="card-header">
        <img src="{{ comment.author.image}}" alt="" class="profil_picture">
      </div>
      <div class="card-body">
        <p class="card-text"><a href="#">{{ comment.author.username}}</a> {{ comment.content }}</p>
      </div>
    </div>
    {% endfor %}
  {% endif %}

  {{form_start(form)}}
  <div class="form-group">
    {{form_row(form.content,{ 'attr' : {
      'class' : 'form-control'
    }})}}
  </div>
  {{form_row(form.submit,{ 'attr' : {
    'class' : 'btn btn-primary'
  }})}}
  {{form_end(form)}}
{% endblock %}
