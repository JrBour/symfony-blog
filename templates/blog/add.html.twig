{% extends 'base.html.twig' %}
{% block body %}
  <h1>Formulaire</h1>
  {{ form_start(form) }}
  {{ form_widget(form) }}
  {{ form_end(form) }}

  <ul>
    {% for category in categories %}
      <li> {{ category.name }}</li>
    {% endfor %}
  </ul>
{% endblock %}
